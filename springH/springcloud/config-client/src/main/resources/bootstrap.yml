spring:
  cloud:
    config:
      name: config-client
      profile: pro
      discovery:
        enabled: true
        serviceId: springcloud-config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
